# Copy to .env.local for local dev against a non-default backend port, or set
# as a Vercel project environment variable (Settings -> Environment Variables)
# pointing at the deployed Render backend, e.g.:
#   VITE_API_BASE_URL=https://risk-premia-api.onrender.com
VITE_API_BASE_URL=http://localhost:8000
